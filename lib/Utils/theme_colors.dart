import 'package:flutter/material.dart';

class ThemeColors {
  // Backgrounds
  static const Color background = Color(0xFF070B19); // Very dark blue/black
  static const Color inputBackground = Color(0xFF0F1522); // Slightly lighter for inputs

  // Brand Colors
  static const Color primaryBlue = Color(0xFF0056FF); // Bright blue for buttons & logos
  
  // Borders
  static const Color inputBorder = Color(0xFF1E253A);
  static const Color dividerColor = Color(0xFF1E253A);

  // Typography
  static const Color textWhite = Colors.white;
  static const Color textGrey = Color(0xFF8B92A5);
  static const Color textDarkGrey = Color(0xFF4A5568);
}